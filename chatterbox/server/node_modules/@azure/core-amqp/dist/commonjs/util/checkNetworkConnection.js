var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var checkNetworkConnection_exports = {};
__export(checkNetworkConnection_exports, {
  checkNetworkConnection: () => checkNetworkConnection
});
module.exports = __toCommonJS(checkNetworkConnection_exports);
var import_node_dns = require("node:dns");
var import_log = require("../log.js");
function checkNetworkConnection(host) {
  return new Promise((resolve) => {
    import_log.logger.verbose("Calling dns.resolve to determine network connection status.");
    (0, import_node_dns.resolve)(host, function(err) {
      if (err) {
        import_log.logger.verbose(
          "Error thrown from dns.resolve in network connection check: '%s', %O",
          err.code || err.name,
          err
        );
        if (err.code === import_node_dns.CONNREFUSED || err.code === import_node_dns.TIMEOUT) {
          return resolve(false);
        }
      } else {
        import_log.logger.verbose("Successfully resolved host via dns.resolve in network connection check.");
      }
      return resolve(true);
    });
  });
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  checkNetworkConnection
});
//# sourceMappingURL=checkNetworkConnection.js.map
